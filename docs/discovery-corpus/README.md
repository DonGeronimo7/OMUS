# Kernel evidence seed

Generated by `mouse_control.kernel_knowledge` from Linux v6.12, resolved to commit
`adc218676eef25575469234709c2d87185ca223a`. Each source node records its full-file
SHA-256 and upstream commit URL; fact nodes record exact line provenance.

These 52 lexical/enum facts are research knowledge, **not capability proof or write
authority**. Symbolic expressions and opaque fields are not decoded. Corpus data
is currently an offline seed, not loaded into normal Discovery or packaged runtime.

To reproduce, obtain each named source at the pinned commit and run, for example:

```sh
PYTHONPATH=src python3 -m mouse_control.kernel_knowledge hid-roccat-common.c \
  --kernel-path drivers/hid/hid-roccat-common.c \
  --revision adc218676eef25575469234709c2d87185ca223a \
  --output /tmp/new-roccat-common.evidence.json
```

The output path must not already exist. Compare with the checked-in snapshot;
never replace source provenance with a moving branch reference.
