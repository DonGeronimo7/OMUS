name: Restore core button-capture ownership

on:
  push:
    branches: [feat/automatic-hardware-discovery]
    paths:
      - tools/restore_button_capture_ownership.py
      - .github/workflows/restore-button-capture-ownership.yml

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/automatic-hardware-discovery
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: pip
      - name: Apply remapping ownership fix
        run: python tools/restore_button_capture_ownership.py
      - name: Install test environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install . pytest
      - name: Core remapping regression gate
        run: |
          PYTHONPATH=src pytest -q \
            tests/test_button_capture_ownership.py \
            tests/test_setup_tui.py \
            tests/test_setup_state_machine.py \
            tests/test_setup_flow.py \
            tests/test_setup_persistence.py \
            tests/test_setup_service.py \
            tests/test_remapper.py \
            tests/test_keyboard_capture.py
      - name: Full regression gate
        run: PYTHONPATH=src pytest -q
      - name: Compile gate
        run: python -m compileall -q src tests
      - name: Whitespace gate
        run: git diff --check
      - name: Commit validated implementation
        run: |
          rm tools/restore_button_capture_ownership.py .github/workflows/restore-button-capture-ownership.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Restore exclusive button capture ownership'
          git push origin HEAD:feat/automatic-hardware-discovery
